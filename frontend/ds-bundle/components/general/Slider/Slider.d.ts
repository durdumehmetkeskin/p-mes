import * as React from 'react';

/**
 * Slider — from frontend@0.1.0.
 * @replaces input[type=range]
 */
export interface SliderProps {
  [key: string]: unknown;
}

export declare const Slider: React.ComponentType<SliderProps>;
