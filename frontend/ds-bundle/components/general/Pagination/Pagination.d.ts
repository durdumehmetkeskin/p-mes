import * as React from 'react';

/**
 * Pagination — from frontend@0.1.0.
 */
export interface PaginationProps {
  [key: string]: unknown;
}

export declare const Pagination: React.ComponentType<PaginationProps>;
