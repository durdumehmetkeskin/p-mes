import * as React from 'react';

/**
 * AlertDialog — from frontend@0.1.0.
 */
export interface AlertDialogProps {
  [key: string]: unknown;
}

export declare const AlertDialog: React.ComponentType<AlertDialogProps>;
