import * as React from 'react';

/**
 * Button — from frontend@0.1.0.
 * @replaces button
 */
export interface ButtonProps {
  [key: string]: unknown;
}

export declare const Button: React.ComponentType<ButtonProps>;
