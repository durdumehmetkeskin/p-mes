# Shared rate-limit zone for the QUA-MES API (goes to /etc/nginx/conf.d/).
# 50 req/s average per client IP; bursts up to 100 are absorbed (mirrors the
# limits the dev Traefik setup used).
limit_req_zone $binary_remote_addr zone=quanta_mes_api:10m rate=50r/s;
