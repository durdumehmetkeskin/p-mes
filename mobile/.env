# API base URL reachable from the device/emulator. localhost only works on
# an emulator running on the same machine as the backend; for a physical
# device use the host's LAN IP (and expose the API beyond 127.0.0.1).
EXPO_PUBLIC_API_URL=http://localhost:3000/api
