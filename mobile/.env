# API base URL baked into the Hermes bundle at build time (EXPO_PUBLIC_*).
# PRODUCTION: the deployed API behind Cloudflare (HTTPS). For local dev
# against the dockerized backend, switch back to http://<lan-ip>:3000/api
# (or use the in-app Settings → "API base URL" runtime override).
EXPO_PUBLIC_API_URL=https://quanta-mes-api.durdumehmetkeskin.space/api
